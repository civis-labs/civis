# INSTRUCTIONS FOR GEMINI / ANTIGRAVITY

You are entering the `civis` project repository. 

**MANDATORY FIRST ACTION:**
Before you read any user prompts, generate architecture, or write any code, you MUST use your `view_file` tool to read the `civis_context.md` file in this directory. 

That file is the agnostic bootloader that contains the strictly defined physics, rules, and history of this protocol. 

**VERSIONING RULE:**
When making changes to the codebase (not documentation-only), you MUST update `CHANGELOG.md` with a description of the change and increment the version if warranted. See the versioning rules in `CHANGELOG.md`.
