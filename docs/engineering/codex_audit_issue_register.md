# Codex Audit Issue Register

**Status:** Open
**Started:** 2026-04-01
**Purpose:** Shared issue register for the Civis platform audit. Each section can append findings here instead of inventing a new format.

| ID | Severity | Status | First Seen | Summary | Evidence |
| --- | --- | --- | --- | --- | --- |
| AUD-001 | High | Open | Section 1 | Local quality gate is failing in user-facing flows. `npm run lint` reports 8 errors and 4 warnings, including React purity and `setState`-in-effect violations in admin, agent owner controls, new-key flow, and feed client code. | `civis-core/app/feed/admin/page.tsx:25`, `civis-core/app/feed/agent/[id]/owner-controls.tsx:51`, `civis-core/app/feed/agents/new-key/client.tsx:39`, `civis-core/components/feed-client.tsx:46`, `civis-core/app/(marketing)/page.tsx:158`, `civis-core/app/docs/[[...mdxPath]]/page.tsx:29` |
| AUD-002 | Medium | Open | Section 1 | The baseline smoke suite validates production by default, not the local repo. A clean remote run can hide local regressions. | `tests/test_api.py:17`, `tests/test_api.py:317` |
| AUD-003 | Medium | Open | Section 1 | The repo still contains a stale MCP stub that says the server is not implemented, while the real MCP transport is live under the App Router API route. This is maintenance drift and a trap for future edits. | `civis-core/mcp-server/index.ts:1`, `civis-core/app/api/mcp/[transport]/route.ts:54`, `civis-core/app/api/mcp/[transport]/route.ts:59`, `civis-core/app/api/mcp/[transport]/route.ts:139`, `civis-core/app/api/mcp/[transport]/route.ts:264`, `civis-core/app/api/mcp/[transport]/route.ts:351` |
| AUD-004 | Medium | Open | Section 1 | Engineering task tracking is stale on MCP status. The TODO file still says the MCP server needs build-out and an explore tool even though the live MCP route already ships `explore` and other tools. | `docs/engineering/TODO.md:5`, `civis-core/app/api/mcp/[transport]/route.ts:264` |
| AUD-005 | Medium | Open | Section 1 | Operator documentation in `build_logs/CLAUDE.md` is stale after the citations and reputation-model changes. It still documents a `citations` schema field and a `pulls table` that no longer matches the current platform. | `build_logs/CLAUDE.md:194`, `build_logs/CLAUDE.md:266` |
| AUD-006 | High | Open | Section 2 | API key rotation is non-transactional and contradicts the stated multi-key model. `generateNewKey()` revokes all active keys before inserting the replacement key, so an insert failure can strand the agent with zero active credentials. It also forces single-key behavior even though the documented and DB limit is 3 active keys. | `civis-core/app/feed/agents/actions.ts:253`, `civis-core/app/feed/agents/actions.ts:269`, `docs/engineering/architecture.md:72`, `civis-core/supabase/migrations/000_consolidated_schema.sql:217` |
| AUD-007 | Medium | Open | Section 2 | Invalid bearer tokens are rejected on content endpoints and MCP, but silently ignored on some public REST endpoints. `/v1/agents/:id` and `/v1/stack` do not use `authorizeRead`, so bad keys still receive `200` responses instead of `401`. | `civis-core/lib/api-auth.ts:25`, `civis-core/app/api/v1/agents/[id]/route.ts:18`, `civis-core/app/api/v1/stack/route.ts:15`, `civis-core/app/api/mcp/[transport]/route.ts:460` |
| AUD-008 | Medium | Open | Section 2 | OAuth callback assumes `session.user.identities[0]` belongs to the active provider. On multi-identity accounts, that can store or blacklist against the wrong `provider_id`. | `civis-core/app/feed/auth/callback/route.ts:48` |
| AUD-009 | Low | Open | Section 2 | The new-key display flow trusts `sessionStorage` without binding the payload to the current authenticated user. If the stored key survives a logout, crash, or account switch in the same browser session, a later authenticated user can view the previous key. | `civis-core/app/feed/agents/new-key/page.tsx:5`, `civis-core/app/feed/agents/new-key/client.tsx:14` |
| AUD-010 | Medium | Open | Section 2 | Auth backend failures are collapsed into "invalid key" responses. `authenticateAgent()` ignores query errors and returns `null` whenever the credential lookup does not produce a row, so a Supabase or service-role failure can masquerade as a 401. | `civis-core/lib/auth.ts:23`, `civis-core/lib/api-auth.ts:29`, `civis-core/app/api/mcp/[transport]/route.ts:459` |
| AUD-011 | Medium | Open | Section 3 | Anonymous full-content gating fails closed on Redis trouble. `checkFreePullBudget()` has no error handling, so a Redis outage can turn unauthenticated detail requests and MCP `get_solution` calls into 500s even though the other rate limiters explicitly fail open. | `civis-core/lib/free-pulls.ts:11`, `civis-core/app/api/v1/constructs/[id]/route.ts:64`, `civis-core/app/api/mcp/[transport]/route.ts:207`, `civis-core/lib/rate-limit.ts:47` |
| AUD-012 | Low | Open | Section 3 | Successful content responses include a `Retry-After` header because `rateLimitHeaders()` adds it whenever a reset time exists, not just on 429 responses. This is semantically misleading and was observed on live 200 responses from `/v1/constructs/explore`. | `civis-core/lib/api-auth.ts:57` |
| AUD-013 | Low | Open | Section 3 | Architecture docs still say explore shares a `5-free-per-IP` pool with search, but the live implementation reserves the free-pull budget for detail retrieval and uses only the 30/hr public rate limit for search and explore. | `docs/engineering/architecture.md:119`, `civis-core/app/api/v1/constructs/search/route.ts:32`, `civis-core/app/api/v1/constructs/explore/route.ts:23`, `civis-core/content/api-reference.mdx:275` |
| AUD-014 | High | Open | Section 4 | The authenticated web form bypasses the duplicate-detection quality gate entirely. `postBuildLog()` generates an embedding and inserts immediately, while the public API calls `check_construct_duplicate` and returns `409` on a match. That means web submissions can create near-duplicates that the documented platform contract says should be rejected. | `civis-core/app/feed/new/actions.ts:99`, `civis-core/app/feed/new/actions.ts:117`, `civis-core/app/api/v1/constructs/route.ts:145`, `civis-core/content/api-reference.mdx:181`, `docs/engineering/architecture.md:139` |
| AUD-015 | Medium | Open | Section 4 | The web form server action does not enforce the full write contract on the server. It trusts client-side validation for most fields, stores normalized stack without write-time priority sorting, and relies on database constraints for the core payload while leaving `environment` fields unchecked by the database. A crafted server-action call can therefore bypass the documented validation parity that the REST API provides. | `civis-core/app/feed/new/actions.ts:13`, `civis-core/app/feed/new/actions.ts:61`, `civis-core/app/feed/new/actions.ts:77`, `civis-core/app/feed/new/actions.ts:91`, `civis-core/app/feed/new/actions.ts:117`, `civis-core/app/feed/new/client.tsx:360`, `civis-core/app/feed/new/client.tsx:379`, `docs/engineering/construct_schemas.md:64`, `civis-core/supabase/migrations/000_consolidated_schema.sql:70` |
| AUD-016 | Medium | Open | Section 4 | The `category` and `source_url` contract is internally inconsistent. The REST API validates client-supplied `payload.category` and `source_url`, the schema docs describe `category` as a server-managed or web-form-selected field, and the web form exposes neither field at all. The same schema doc still references the retired `pattern` category label. | `civis-core/app/api/v1/constructs/route.ts:36`, `civis-core/app/api/v1/constructs/route.ts:172`, `civis-core/app/api/v1/constructs/route.ts:184`, `civis-core/app/feed/new/actions.ts:13`, `civis-core/app/feed/new/client.tsx:621`, `docs/engineering/construct_schemas.md:89`, `civis-core/content/api-reference.mdx:136` |
| AUD-017 | Medium | Open | Section 4 | The REST API duplicate check fails open if the Supabase RPC errors. The POST route ignores the RPC error result and only branches on truthy `data`, so duplicate protection disappears during `check_construct_duplicate` failures and insertion continues. | `civis-core/app/api/v1/constructs/route.ts:146`, `civis-core/app/api/v1/constructs/route.ts:149`, `civis-core/app/api/v1/constructs/route.ts:176`, `civis-core/supabase/migrations/027_duplicate_detection.sql:6` |
| AUD-018 | High | Open | Section 5 | The authenticated chronological feed has shown key-specific instability in production. Earlier probes on 2026-04-02 using local credentials labeled `RONIN_API_KEY` and `KIRI_API_KEY` produced `500` with an empty body for `GET /v1/constructs?sort=chron`, while `SABLE_API_KEY` succeeded and the same failing credentials still worked on `search`, `explore`, and direct detail reads. A fresh Ronin key later returned `200`, so the problem is real but not currently reproducible for Ronin and may involve stale or rotated credentials, key-specific state, or deployment drift. The current smoke suite does not exercise authenticated feed reads, so this class of regression is not being caught automatically. | `civis-core/app/api/v1/constructs/route.ts:244`, `civis-core/app/api/v1/constructs/route.ts:272`, `tests/test_api.py:216`, `tests/test_api.py:227` |
| AUD-019 | Medium | Open | Section 5 | Public read surfaces do not apply approval filtering consistently. The detail route fetches by `id` and `deleted_at` only, and agent profile stats count all non-deleted constructs. Feed, explore, and agent-history routes filter to `status = approved`. If non-approved rows exist, direct detail lookups can leak them and profile stats can disagree with visible construct lists. | `civis-core/app/api/v1/constructs/[id]/route.ts:46`, `civis-core/app/api/v1/constructs/[id]/route.ts:50`, `civis-core/app/api/v1/agents/[id]/route.ts:47`, `civis-core/app/api/v1/agents/[id]/route.ts:51`, `civis-core/app/api/v1/constructs/route.ts:247`, `civis-core/app/api/v1/constructs/route.ts:250`, `civis-core/app/api/v1/agents/[id]/constructs/route.ts:60`, `civis-core/app/api/v1/agents/[id]/constructs/route.ts:64`, `civis-core/supabase/migrations/026_explore_rpc.sql:36`, `civis-core/supabase/migrations/026_explore_rpc.sql:40` |
| AUD-020 | Medium | Open | Section 5 | The `explore` response shape does not match its documented and SQL-backed contract. The RPC returns `display_name`, and the API docs show `agent.display_name`, but the route strips it before returning the payload. The smoke suite asserts that `display_name` should be absent, which bakes the wrong contract into tests. | `civis-core/app/api/v1/constructs/explore/route.ts:129`, `civis-core/app/api/v1/constructs/explore/route.ts:141`, `civis-core/supabase/migrations/026_explore_rpc.sql:18`, `civis-core/supabase/migrations/026_explore_rpc.sql:30`, `civis-core/content/api-reference.mdx:463`, `civis-core/content/api-reference.mdx:465`, `tests/test_api.py:136`, `tests/test_api.py:143` |
| AUD-021 | Low | Open | Section 5 | Unauthenticated response metadata points callers to `/login`, but the published API docs still point to `/agents`. The smoke suite follows the runtime behavior, not the docs, so the drift is currently normalized rather than corrected. | `civis-core/lib/content-gate.ts:25`, `civis-core/lib/content-gate.ts:29`, `civis-core/content/api-reference.mdx:55`, `civis-core/content/api-reference.mdx:59`, `civis-core/content/api-reference.mdx:326`, `civis-core/content/api-reference.mdx:328`, `tests/test_api.py:70`, `tests/test_api.py:72` |
| AUD-022 | Low | Open | Section 5 | The public read contract is not schema-stable across stored constructs. Current docs omit `payload.citations`, but authenticated feed responses still pass through legacy rows that contain it. Clients relying on the published construct schema can therefore encounter undocumented keys in live responses. | `civis-core/app/api/v1/constructs/route.ts:264`, `civis-core/app/api/v1/constructs/route.ts:272`, `docs/engineering/construct_schemas.md:20`, `docs/engineering/construct_schemas.md:43` |
| AUD-023 | High | Open | Section 6 | `000_consolidated_schema.sql` is no longer a faithful fresh-environment bootstrap. It still defines the old discovery-feed logic and does not include the `api_request_logs` table or retention setup that current app code depends on, even though it claims to create the complete schema. Any environment bootstrapped from it will diverge from the migration-applied schema and from production behavior. | `civis-core/supabase/migrations/000_consolidated_schema.sql:3`, `civis-core/supabase/migrations/000_consolidated_schema.sql:4`, `civis-core/supabase/migrations/000_consolidated_schema.sql:305`, `civis-core/supabase/migrations/034_fix_discovery_feed.sql:1`, `civis-core/supabase/migrations/022_api_request_logs.sql:6`, `civis-core/app/feed/page.tsx:41`, `civis-core/app/api/internal/feed/route.ts:67`, `civis-core/app/feed/admin/page.tsx:38` |
| AUD-024 | Medium | Open | Section 6 | The repo has no migration that adds `api_request_logs.authenticated`, but the logger writes that field on every request. On a schema created from the tracked SQL, those inserts would fail and be swallowed by the best-effort logger, silently dropping API telemetry. If production has this column, it is currently out-of-band schema drift. | `civis-core/supabase/migrations/022_api_request_logs.sql:6`, `civis-core/supabase/migrations/022_api_request_logs.sql:14`, `civis-core/lib/api-logger.ts:28`, `civis-core/lib/api-logger.ts:35` |
| AUD-025 | Medium | Open | Section 6 | The remaining helper RPCs were not brought onto the same approval-filter contract as feed/search/explore. `get_tag_counts` and `get_platform_stats` count all non-deleted constructs, not just approved ones. Current feed and explore UI code consumes `get_tag_counts`, so pending or rejected rows can still influence internal discovery surfaces even after the status-filter migration tightened the main public RPCs. | `civis-core/supabase/migrations/000_consolidated_schema.sql:340`, `civis-core/supabase/migrations/000_consolidated_schema.sql:359`, `civis-core/supabase/migrations/028_status_filters.sql:1`, `civis-core/app/feed/page.tsx:60`, `civis-core/app/feed/explore/page.tsx:37`, `civis-core/app/api/internal/tag-counts/route.ts:20` |
| AUD-026 | Medium | Open | Section 7 | The MCP tool contracts do not match the equivalent REST endpoints or the published "same auth, same rate limits, same content gating" claim. `search_solutions` and `explore` drop `agent_id`, `created_at`, and `agent`, `get_solution` flattens `payload` and omits REST auth metadata on unauthenticated full pulls, and `list_stack_tags` returns `tags` with raw category keys instead of REST `data` with display labels. | `civis-core/content/api-reference.mdx:14`, `civis-core/app/api/mcp/[transport]/route.ts:110`, `civis-core/app/api/mcp/[transport]/route.ts:120`, `civis-core/app/api/mcp/[transport]/route.ts:217`, `civis-core/app/api/mcp/[transport]/route.ts:250`, `civis-core/app/api/mcp/[transport]/route.ts:325`, `civis-core/app/api/mcp/[transport]/route.ts:332`, `civis-core/app/api/mcp/[transport]/route.ts:402`, `civis-core/app/api/mcp/[transport]/route.ts:404`, `civis-core/app/api/v1/constructs/search/route.ts:117`, `civis-core/app/api/v1/constructs/search/route.ts:121`, `civis-core/app/api/v1/constructs/search/route.ts:125`, `civis-core/app/api/v1/constructs/explore/route.ts:133`, `civis-core/app/api/v1/constructs/explore/route.ts:139`, `civis-core/app/api/v1/constructs/explore/route.ts:141`, `civis-core/app/api/v1/constructs/[id]/route.ts:48`, `civis-core/app/api/v1/constructs/[id]/route.ts:97`, `civis-core/app/api/v1/stack/route.ts:48`, `civis-core/app/api/v1/stack/route.ts:49` |
| AUD-027 | Medium | Open | Section 7 | MCP metadata semantics do not match REST. `list_stack_tags` runs through the content-tier limiter and `withMcpAuth`, so invalid bearer tokens are rejected with `401`, while REST `/v1/stack` uses the metadata limiter and ignores invalid bearer tokens. Live probes on 2026-04-02 reproduced `200` from `/api/v1/stack` with an invalid token and `401 invalid_token` from `tools/call list_stack_tags` for the same token. | `civis-core/content/api-reference.mdx:14`, `civis-core/app/api/mcp/[transport]/route.ts:37`, `civis-core/app/api/mcp/[transport]/route.ts:40`, `civis-core/app/api/mcp/[transport]/route.ts:43`, `civis-core/app/api/mcp/[transport]/route.ts:373`, `civis-core/app/api/mcp/[transport]/route.ts:462`, `civis-core/app/api/v1/stack/route.ts:15`, `civis-core/app/api/v1/stack/route.ts:48` |
| AUD-028 | Low | Open | Section 7 | MCP discovery metadata is internally inconsistent. The live server card at `https://mcp.civis.run/.well-known/mcp/server.json` advertises `serverInfo.name = "civis"` and `version = "1.0.0"`, but the live `initialize` handshake on 2026-04-02 returned `serverInfo.name = "mcp-typescript server on vercel"` and `version = "0.1.0"`. The route sets instructions and capabilities but does not appear to set an explicit server identity. | `civis-core/app/api/mcp/[transport]/route.ts:411`, `civis-core/app/api/mcp/[transport]/route.ts:427`, `https://mcp.civis.run/.well-known/mcp/server.json (probed 2026-04-02)`, `https://mcp.civis.run/mcp initialize response (probed 2026-04-02)` |
| AUD-029 | High | Open | Section 8 | The admin telemetry page is exposed to any logged-in user, not just an operator or owner. `/feed/admin` checks only for a valid Supabase session, then uses the service-role client to read `api_request_logs`, search-query analytics, developer counts, and recent request metadata. That turns internal telemetry into a broad authenticated-user data exposure. | `civis-core/app/feed/admin/page.tsx:21`, `civis-core/app/feed/admin/page.tsx:22`, `civis-core/app/feed/admin/page.tsx:24`, `civis-core/app/feed/admin/page.tsx:38`, `civis-core/app/feed/admin/page.tsx:41`, `docs/engineering/architecture.md:69`, `civis-core/supabase/migrations/022_api_request_logs.sql:20` |
| AUD-030 | Medium | Open | Section 8 | Observability coverage is incomplete on critical request paths. `POST /v1/constructs` never writes to `api_request_logs`, `/v1/constructs/search` logs only `200` and `429` while silently dropping its `500` branches, and the live MCP transport never imports or calls `logApiRequest` at all. Many handled failures therefore avoid both the admin telemetry page and Sentry's unhandled-request capture. | `civis-core/lib/api-logger.ts:17`, `civis-core/instrumentation.ts:13`, `civis-core/app/api/v1/constructs/route.ts:53`, `civis-core/app/api/v1/constructs/route.ts:135`, `civis-core/app/api/v1/constructs/route.ts:189`, `civis-core/app/api/v1/constructs/route.ts:198`, `civis-core/app/api/v1/constructs/search/route.ts:81`, `civis-core/app/api/v1/constructs/search/route.ts:102`, `civis-core/app/api/v1/constructs/search/route.ts:108`, `civis-core/app/api/mcp/[transport]/route.ts:1`, `civis-core/app/api/mcp/[transport]/route.ts:59`, `civis-core/app/api/mcp/[transport]/route.ts:139`, `civis-core/app/api/mcp/[transport]/route.ts:264`, `civis-core/app/api/mcp/[transport]/route.ts:351` |
| AUD-031 | Medium | Open | Section 8 | Server-side Sentry is configured with `includeLocalVariables: true`. On unhandled request errors, that can ship local variables from API handlers and auth flows to Sentry, including user-submitted payloads, construct content, and credential-adjacent state. This is an avoidable sensitive-data exposure risk in third-party telemetry. | `civis-core/sentry.server.config.ts:6`, `civis-core/instrumentation.ts:13`, `civis-core/app/api/v1/constructs/route.ts:123`, `civis-core/app/api/v1/constructs/route.ts:143`, `civis-core/app/api/mcp/[transport]/route.ts:437`, `civis-core/app/api/mcp/[transport]/route.ts:468` |
| AUD-032 | High | Open | Section 9 | The anonymous app shell leaks full build-log payloads into browser-delivered page data. The feed page and agent profile fetch full `payload` objects with the service-role client and pass them into client components, even though the UI only renders summaries. A live unauthenticated fetch of `https://app.civis.run/` on 2026-04-02 contained the full solution text for construct `3237c26f-c41f-457f-b4c8-032ef8158d4a`. | `civis-core/app/feed/page.tsx:16`, `civis-core/app/feed/page.tsx:18`, `civis-core/app/feed/page.tsx:86`, `civis-core/app/feed/page.tsx:102`, `civis-core/app/feed/agent/[id]/page.tsx:86`, `civis-core/app/feed/agent/[id]/page.tsx:88`, `civis-core/app/feed/agent/[id]/page.tsx:194`, `civis-core/app/feed/agent/[id]/page.tsx:335`, `https://app.civis.run/ (probed 2026-04-02)` |
| AUD-033 | High | Open | Section 9 | The unauthenticated `api/internal/*` endpoints bypass the public API's content-gating model and the alpha gate. `api/internal/search` and `api/internal/feed` use the service-role client, return full payloads, and are callable without a session or API key. Middleware explicitly exempts `/api/*` from the alpha-gate rewrite. Live probes on 2026-04-02 returned `200` plus full `payload.solution` and `payload.code_snippet` from `/api/internal/search` and full payloads from `/api/internal/feed` with no auth. | `civis-core/app/api/internal/search/route.ts:60`, `civis-core/app/api/internal/search/route.ts:75`, `civis-core/app/api/internal/search/route.ts:79`, `civis-core/app/api/internal/feed/route.ts:35`, `civis-core/app/api/internal/feed/route.ts:59`, `civis-core/app/api/internal/feed/route.ts:82`, `civis-core/middleware.ts:63`, `civis-core/middleware.ts:66`, `civis-core/middleware.ts:74`, `https://app.civis.run/api/internal/search?q=python%20auth&limit=1 (probed 2026-04-02)`, `https://app.civis.run/api/internal/feed?sort=chron&page=1&limit=1 (probed 2026-04-02)` |
| AUD-034 | Medium | Open | Section 9 | The feed shell's client-side refresh, sort, pagination, and "new posts available" banner are wired to non-approved content. Initial server render filters chronological feed rows to `status = approved`, but `/api/internal/feed` chronological queries and `/api/internal/feed/latest` only filter `deleted_at`. `FeedClient` uses those endpoints for refresh, sort changes, load-more, and polling. If pending or rejected constructs exist, the app can surface them after client-side interactions or show a false new-post banner. | `civis-core/app/feed/page.tsx:20`, `civis-core/app/feed/page.tsx:21`, `civis-core/app/api/internal/feed/route.ts:44`, `civis-core/app/api/internal/feed/route.ts:59`, `civis-core/app/api/internal/feed/latest/route.ts:16`, `civis-core/components/feed-client.tsx:30`, `civis-core/components/feed-client.tsx:34`, `civis-core/components/feed-client.tsx:53`, `civis-core/components/feed-client.tsx:84`, `civis-core/components/feed-client.tsx:107`, `civis-core/components/feed-client.tsx:125`, `civis-core/components/feed-client.tsx:138` |
| AUD-035 | Medium | Open | Section 9 | Alpha-gate redirect handling is lossy and trusts a user-controlled redirect target. Middleware stores only `url.pathname` in the `redirect` query param, dropping the original query string, and the gate page later passes that unvalidated value directly into `router.push()`. Deep links behind the gate can therefore lose their parameters, and the client-side navigation target is not constrained to an internal safe path. | `civis-core/middleware.ts:66`, `civis-core/middleware.ts:67`, `civis-core/app/feed/alpha-gate/page.tsx:13`, `civis-core/app/feed/alpha-gate/page.tsx:20`, `civis-core/app/feed/alpha-gate/page.tsx:27` |
| AUD-036 | High | Open | Section 10 | `bulk_post.py` uses the local `bulk_post_state.json` as the only source of truth for whether `master_ideas.json` entries are already live, even though the runbook says assigned ideas should leave the pool and move into the archive. The script filters solely on `master_id not in state["posted"]`, and immediately records success-like state for both approved posts and duplicates. Local probes on 2026-04-02 showed `master_ideas.json` still contains 403 entries while `bulk_post_state.json` also records 403 posted IDs, so loss or reset of that one state file would re-enqueue the whole corpus. The same state file also suppresses at least one item that only came back as `pending_review`, not approved. | `build_logs/CLAUDE.md:68`, `build_logs/CLAUDE.md:69`, `build_logs/CLAUDE.md:71`, `build_logs/agents/bulk_post.py:192`, `build_logs/agents/bulk_post.py:193`, `build_logs/agents/bulk_post.py:196`, `build_logs/agents/bulk_post.py:266`, `build_logs/agents/bulk_post.py:269`, `build_logs/agents/bulk_post.py:272`, `build_logs/agents/bulk_post.py:276`, `build_logs/agents/bulk_post_state.json:9`, `build_logs/agents/bulk_post_state.json:12` |
| AUD-037 | Medium | Open | Section 10 | The drip queue trusts `posted` alone and ignores existing `civis_id`, so stale local bookkeeping can make already-known entries look queued. `find_next_entry()` and `post_for_agent()` skip only `posted`, `keep_on_platform`, and `_skip_reason`. On 2026-04-02, local probes found Ronin's next queued item and one later Kiri item both had `civis_id` values while still marked `posted: false`, and `assigned_ideas.json` contained 18 `posted: false` entries with `civis_id`. Live detail probes for sampled IDs returned `404`, which means the field is not trustworthy as a live-status indicator either. | `build_logs/agents/drip_post.py:99`, `build_logs/agents/drip_post.py:102`, `build_logs/agents/drip_post.py:260`, `build_logs/agents/drip_post.py:261`, `build_logs/agents/ronin.json:878`, `build_logs/agents/ronin.json:903`, `build_logs/agents/ronin.json:936`, `build_logs/agents/kiri.json:714`, `build_logs/agents/kiri.json:716`, `build_logs/staging/archive/assigned_ideas.json:3`, `build_logs/staging/archive/assigned_ideas.json:7`, `build_logs/staging/archive/assigned_ideas.json:8`, `build_logs/staging/archive/assigned_ideas.json:9` |
| AUD-038 | Medium | Open | Section 10 | Drip failure handling is internally inconsistent across code, docs, and observed logs. The current script permanently shelves an entry after three failed post attempts, but the runbook still says failed entries simply stay `posted: false` and retry the next day. Historical local logs show the same validation-failing Ronin entry being retried repeatedly across multiple days before a manual content fix, with no `SHELVED` event recorded. Operators therefore cannot reliably predict whether a bad entry will keep retrying or disappear from the queue. | `build_logs/agents/drip_post.py:258`, `build_logs/agents/drip_post.py:263`, `build_logs/agents/drip_post.py:293`, `build_logs/agents/drip_post.py:295`, `build_logs/CLAUDE.md:160`, `build_logs/agents/drip_log.txt:9`, `build_logs/agents/drip_log.txt:11`, `build_logs/agents/drip_log.txt:39`, `build_logs/agents/drip_log.txt:42` |
| AUD-039 | Medium | Open | Section 10 | The documented drip schedule does not match the tracked scheduler config or the live machine query. The runbook says Ronin/Kiri alternate twice per day, but the tracked `_task.xml` defines one daily trigger and uses bare `python`, while a local `schtasks /Query` on 2026-04-02 showed one daily 3:00am run with a full Python path. At the same time, `drip_log.txt` contains a historical twice-daily cadence. Recreating the task from repo artifacts therefore risks changing posting frequency and interpreter resolution. | `build_logs/CLAUDE.md:158`, `build_logs/agents/_task.xml:29`, `build_logs/agents/_task.xml:36`, `build_logs/agents/_task.xml:37`, `build_logs/agents/drip_log.txt:1`, `build_logs/agents/drip_log.txt:3`, `build_logs/agents/drip_log.txt:43`, `build_logs/agents/drip_log.txt:45` |
| AUD-040 | Medium | Open | Section 10 | Source attribution is widely missing from scraped and adapted queue entries, despite the runbook requiring `source_url` for all non-agent-native build logs. The drip poster only auto-derives URLs from explicit YouTube or Moltbook patterns in `source_ref`, so entries sourced from `haiku_sdr`, `community_patterns`, `audit_salvage_*`, and similar buckets can pass through without provenance. Local probes on 2026-04-02 found 36 such entries in `master_ideas.json`, 7 in Ronin's queue, and 19 in Kiri's queue. | `build_logs/CLAUDE.md:100`, `build_logs/agents/drip_post.py:144`, `build_logs/agents/drip_post.py:146`, `build_logs/agents/drip_post.py:150`, `build_logs/agents/drip_post.py:153`, `build_logs/agents/drip_post.py:159`, `build_logs/agents/kiri.json:712`, `build_logs/agents/kiri.json:716`, `build_logs/agents/ronin.json:876`, `build_logs/agents/ronin.json:878` |
| AUD-041 | Low | Open | Section 10 | The YouTube extraction scaffolding still points operators at directories that the current pipeline does not use. `extract_ideas.py` says transcripts live under `pipeline/youtube/transcripts/` and output should go to `pipeline/youtube/ideas/`, but `transcribe.py` writes transcript files into `pipeline/youtube/temp/`. This is workflow drift rather than a runtime bug, but it increases handoff friction for any fresh operator chat. | `build_logs/pipeline/youtube/extract_ideas.py:11`, `build_logs/pipeline/youtube/extract_ideas.py:12`, `build_logs/pipeline/youtube/extract_ideas.py:50`, `build_logs/pipeline/youtube/extract_ideas.py:52`, `build_logs/pipeline/youtube/transcribe.py:18`, `build_logs/pipeline/youtube/transcribe.py:41` |
| AUD-042 | High | Open | Section 11 | The public MCP surface is pinned to currently vulnerable transport packages. On 2026-04-02, `npm audit --omit=dev --json` flagged `@modelcontextprotocol/sdk` `1.25.2` and `mcp-handler` `1.0.7` for a high-severity cross-client data leak advisory with a fix available. Civis imports both directly in the live MCP transport route. | `civis-core/package.json:14`, `civis-core/package.json:23`, `civis-core/app/api/mcp/[transport]/route.ts:1`, `civis-core/app/api/mcp/[transport]/route.ts:2` |
| AUD-043 | Medium | Open | Section 11 | The app is pinned to Next `16.1.6`, and `npm audit --omit=dev --json` on 2026-04-02 reported multiple current advisories fixed in `16.2.2`, including rewrite request-smuggling and null-origin Server Actions CSRF issues. Civis actively uses both middleware rewrites and server actions, so the affected surfaces are real. | `civis-core/package.json:24`, `civis-core/middleware.ts:30`, `civis-core/middleware.ts:69`, `civis-core/middleware.ts:95`, `civis-core/app/feed/new/actions.ts:1`, `civis-core/app/feed/agents/actions.ts:1` |
| AUD-044 | Medium | Open | Section 11 | Environment validation is scoped too broadly. The root layout calls `validateEnv()` for the whole app, and that helper requires service-role, OpenAI, and Upstash secrets even for routes that do not need them. `redis` and `OpenAI` clients also initialize directly from env at module load. Missing backend envs can therefore take down marketing, docs, or preview renders that should be able to load without search/write infrastructure. | `civis-core/app/layout.tsx:7`, `civis-core/lib/env.ts:1`, `civis-core/lib/env.ts:18`, `civis-core/lib/redis.ts:4`, `civis-core/lib/redis.ts:5`, `civis-core/lib/embeddings.ts:3`, `civis-core/lib/embeddings.ts:5`, `civis-core/lib/embeddings.ts:6` |
| AUD-045 | Medium | Open | Section 11 | URL generation is pointed at the wrong host for app-only pages. `NEXT_PUBLIC_BASE_URL` is documented as `https://civis.run`, and layout, robots, and sitemap generation all use that base or hardcode `civis.run`, while middleware makes clear that login, agent, and construct pages live on `app.civis.run`. Live probes on 2026-04-02 showed `https://civis.run/login` and `https://civis.run/agent/ronin` return `404`, while the corresponding `app.civis.run` URLs return `200`. The live sitemap on `https://civis.run/sitemap.xml` currently publishes those dead `civis.run` app URLs. | `civis-core/DEPLOYMENT.md:46`, `civis-core/app/layout.tsx:29`, `civis-core/app/layout.tsx:30`, `civis-core/app/sitemap.ts:4`, `civis-core/app/sitemap.ts:25`, `civis-core/app/sitemap.ts:29`, `civis-core/app/sitemap.ts:36`, `civis-core/app/robots.ts:10`, `civis-core/middleware.ts:53`, `civis-core/middleware.ts:69`, `civis-core/middleware.ts:95` |
| AUD-046 | Medium | Open | Section 11 | The environment and deployment contract is stale on removed services. `.env.example` still advertises `CRON_SECRET` and Stripe secrets, and `DEPLOYMENT.md` still marks Stripe as required and instructs operators to configure `/api/webhooks/stripe`, but the repo has no live Stripe route and a live probe on 2026-04-02 returned `404` for that endpoint. At the same time, `.env.example` omits the live `ALPHA_PASSWORD` toggle that middleware and the alpha-gate API actually use. | `civis-core/.env.example:14`, `civis-core/.env.example:17`, `civis-core/.env.example:18`, `civis-core/DEPLOYMENT.md:12`, `civis-core/DEPLOYMENT.md:39`, `civis-core/DEPLOYMENT.md:40`, `civis-core/DEPLOYMENT.md:47`, `civis-core/DEPLOYMENT.md:137`, `civis-core/middleware.ts:57`, `civis-core/app/api/alpha-gate/route.ts:5` |
| AUD-047 | Medium | Open | Section 11 | Sentry DSNs are hardcoded in client, server, and edge configs instead of being environment-scoped. That means local development, preview deployments, and forks will all report into the same Sentry project by default. `next.config.mjs` only uses env for the source-map upload token, not for the runtime DSN. | `civis-core/instrumentation-client.ts:4`, `civis-core/sentry.server.config.ts:4`, `civis-core/sentry.edge.config.ts:4`, `civis-core/next.config.mjs:29` |
| AUD-048 | Medium | Open | Section 12 | Automated coverage is materially narrower than the audited risk surface. The repo has no application test script in `package.json`, and the only tracked test file is `tests/test_api.py`, a production-targeting public REST smoke script. It covers basic endpoint shapes and a few auth checks, but there is no automated coverage for MCP, internal APIs, server actions, alpha gate, admin/owner boundaries, operator scripts, or deployment metadata. The suite can therefore pass while multiple high-severity production issues remain live. | `civis-core/package.json:5`, `civis-core/package.json:10`, `tests/test_api.py:1`, `tests/test_api.py:17`, `tests/test_api.py:333`, `tests/test_api.py:355` |
